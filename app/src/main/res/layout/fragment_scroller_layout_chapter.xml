<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent">

    <ScrollView
        android:id="@+id/scrollview_content_chapter_detail"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent">

        <TextView
            android:id="@+id/textview_content_chapter_detail"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:lineSpacingMultiplier="1.6"
            android:linksClickable="false"
            android:paddingLeft="@dimen/large_space"
            android:paddingTop="?actionBarSize"
            android:paddingRight="@dimen/large_space"
            android:paddingBottom="?actionBarSize"
            android:textColor="@color/secondText"
            android:textSize="18.0sp" />
    </ScrollView>

    <TextView
        android:id="@+id/textview_chapter_count"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_alignParentBottom="true"
        android:layout_marginStart="@dimen/large_space"
        android:textColor="@color/secondText"
        android:textSize="10.0sp" />

    <TextView
        android:id="@+id/textview_next_chapter"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:background="@drawable/main_bg_border"
        android:paddingLeft="@dimen/large_space"
        android:paddingTop="@dimen/medium_space"
        android:paddingRight="@dimen/large_space"
        android:paddingBottom="@dimen/medium_space"
        android:text="@string/next_chapter"
        android:textColor="@color/mainText"
        android:textSize="@dimen/medium_text_size" />
</RelativeLayout>